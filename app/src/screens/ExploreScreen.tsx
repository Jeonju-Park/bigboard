import Screen, { ScreenPlaceholder } from '@/shared/components/Screen'

/** S5 탐색 — 통합검색 + 인기 큰손 + 최근 본 항목 (STEP 4-5) */
export default function ExploreScreen() {
  return (
    <Screen title="탐색">
      <ScreenPlaceholder name="탐색" note="종목·인물 통합검색과 인기 큰손 목록이 들어갑니다." />
    </Screen>
  )
}
