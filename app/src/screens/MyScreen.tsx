import Screen, { ScreenPlaceholder } from '@/shared/components/Screen'

/** S8 마이 — 팔로우 관리 / 증권사 설정 / 출처·고지 (STEP 4-8) */
export default function MyScreen() {
  return (
    <Screen title="마이">
      <ScreenPlaceholder name="마이" note="팔로우 관리, 증권사 설정, 데이터 출처와 고지가 들어갑니다." />
    </Screen>
  )
}
