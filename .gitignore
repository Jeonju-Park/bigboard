node_modules/
dist/
build/
# API 키 — 절대 커밋 금지 (CLAUDE.md 규칙 5). 하위 폴더의 pipeline/.env 도 이 패턴에 걸린다
.env
.env.*
!.env.example
.DS_Store
*.log
.expo/
coverage/
# 대용량 원본 애셋은 git 제외 (내보내기 결과물만 커밋)
assets/figma-exports/raw/

# 생성물 — tokens.ts 에서 자동 생성되므로 커밋하지 않는다
app/src/theme/tokens.generated.css

# 탐색 단계의 원본 샘플 (커밋하지 않음)
pipeline/.explore/

# 원문 캐시 (재실행 비용을 0으로 만들지만 커밋하진 않는다)
pipeline/.cache/

# 공직자 원본 자료 (유저가 내려받아 넣는 파일 — 커밋하지 않는다)
pipeline/data/

# 시세 일자별 캐시
pipeline/.cache/prices/

# 관보 PDF 캐시 — 재파싱할 때 남의 서버를 다시 두드리지 않으려고 둔다 (커밋하지 않음)
pipeline/.gazette-cache/
